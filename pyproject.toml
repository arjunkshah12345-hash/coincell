[project]
name = "haloscan"
version = "2.3.0"
description = "Haloscan — button battery vs coin pediatric X-ray AI (Reese's Law diagnostic gap)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
haloscan = "app:main"

[tool.setuptools.packages.find]
include = ["haloscan*", "api*"]
